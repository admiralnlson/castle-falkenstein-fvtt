<form class="{{cssClass}}" autocomplete="off">

  <header class="sheet-header">
    <div class="header-fields">
      <div class="flexcol">
        <div class="grid weapon-sheet-header1">
          <label class="resource-label">{{localize 'castle-falkenstein.weapon.weapon'}}:</label>
          <input name="name" type="text" value="{{item.name}}" placeholder="Name" />
          <label class="resource-label">{{localize 'castle-falkenstein.weapon.effectiveRange'}}:</label>
          <input class="range" name="system.effectiveRange" type="text" value="{{system.effectiveRange}}" placeholder="" />
        </div>
        <div class="grid weapon-sheet-header2">
          <span class="separator"></span>
          <label class="resource-label">{{localize 'castle-falkenstein.weapon.ammunition'}}:</label>
          <input class="ammunition-current" name="system.ammunition" type="number" min="0" value="{{system.ammunition}}" placeholder="" />
          <span>/</span>
          <input class="ammunition-max" name="system.ammunition_max" type="number" min="0" value="{{system.ammunition_max}}" placeholder="" />
          <span class="separator"></span>
          <label class="resource-label">{{localize 'castle-falkenstein.weapon.wounds'}}:</label>
          <input class="wounds-partial" name="system.woundsPartial" type="number" min="0" value="{{system.woundsPartial}}" placeholder="" />
          <span>/</span>
          <input class="wounds-full" name="system.woundsFull" type="number" min="0" value="{{system.woundsFull}}" placeholder="" />
          <span>/</span>
          <input class="wounds-high" name="system.woundsHigh" type="number" min="0" value="{{system.woundsHigh}}" placeholder="" />
          <span class="separator"></span>
        </div>
      </div>
    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="tab-link" data-tab="description">{{localize 'castle-falkenstein.description'}}</a>
    <a class="tab-link" data-tab="icon">{{localize 'castle-falkenstein.icon'}}</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">

    {{!-- Description Tab --}}
    <div class="tab" data-group="primary" data-tab="description">
      {{!-- To render inline rolls in a TinyMCE editor, you need to pass the parent actor's (if any) roll data to the rollData prop. --}}
      {{editor enrichedDescription target="system.description" button=true editable=editable engine="prosemirror"}}
    </div>

    {{!-- Icon Tab --}}
    <div class="tab" data-group="primary" data-tab="icon">
      <center><img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" height="100" width="100"/></center>
    </div>

  </section>
</form>
